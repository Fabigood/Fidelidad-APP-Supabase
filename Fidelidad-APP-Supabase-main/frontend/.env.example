# URL de la API. Dejala vacía para usar rutas relativas (/api) con nginx
# haciendo de proxy hacia el backend. Solo definila si el backend está en
# otro dominio.
#
# VITE_API_URL=https://api.tudominio.com/api
VITE_API_URL=
